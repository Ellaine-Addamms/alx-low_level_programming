#include "main.h"

/**
 * print_alphabet - Print out the alphabet
 * main - uses built in function
 * Return: 0 (success)
 */

void print_alphabet(void)
{
char i;
for (i = 'a'; i <= 'z'; i++)
_putchar(i);
_putchar('\n');
}
